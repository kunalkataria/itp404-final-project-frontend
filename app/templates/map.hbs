<div style="margin-left:20%">
  <div id="map"></div>
  <h2>Map</h2>

  {{#g-map markersFitMode='live' as |context|}}
    {{#each model as |restaraunt|}}
      {{g-map-address-marker context onClick=(action "handleClick" restaraunt) address=restaraunt.location}}
    {{/each}}

  {{/g-map}}

  Details:
  {{details}}
</div>



{{outlet}}
